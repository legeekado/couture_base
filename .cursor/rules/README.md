# Règles Cursor

## Racine Kalmy (ce dépôt)

- `captures-interfaces.mdc` — scripts Playwright/ADB, prérequis services, parcours et pièges connus

## Sous-projets (dépôts séparés)

Les autres `.mdc` sont dans chaque repo :

- `backend/.cursor/rules/` → repo `couture_backend`
- `mobile/.cursor/rules/` → repo `couture_mobile`
- `backoffice/.cursor/rules/` → repo `couture_backoffice`
